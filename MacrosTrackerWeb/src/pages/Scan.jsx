import PageShell from "../components/PageShell";

function ScanPage() {
  return (
    <PageShell
      eyebrow="Core Flow"
      title="Scan placeholder"
      description="Spec 2 will add camera capture, upload preview, and AI nutrition results."
    >
      <p>This is the future home of the scan-first experience from the business context.</p>
    </PageShell>
  );
}

export default ScanPage;
