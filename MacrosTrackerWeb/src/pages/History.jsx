import PageShell from "../components/PageShell";

function HistoryPage() {
  return (
    <PageShell
      eyebrow="Analytics"
      title="History placeholder"
      description="Spec 4 will add weekly trends and paginated daily summaries."
    >
      <p>The route and layout are wired so the next feature phase can drop in cleanly.</p>
    </PageShell>
  );
}

export default HistoryPage;
